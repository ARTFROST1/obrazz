# GlassDropdownMenu — Custom dropdown (по умолчанию) и опциональный Native UIMenu

## 🎯 Текущее состояние

**✅ Работает СЕЙЧАС:** Native UIMenu на iOS (после development build)

- На iOS автоматически используется нативный UIMenu
- На Android fallback на custom dropdown
- Все функции (icons, destructive, disabled) поддерживаются

**Требования для нативного UIMenu:**

- Development build (не Expo Go)
- iOS 13+
- Пакет `@react-native-menu/menu` v2.0.0+ (уже установлен)

---

## 🔧 Как переключиться на нативный UIMenu

### Шаг 1: Соберите нативный билд

#### Вариант A: EAS Build (требует Apple Developer)

```bash
npx eas build --platform ios --profile development
```

#### Вариант B: Local build на Mac

```bash
cd ios
pod install
cd ..
npx expo run:ios
```

### Шаг 2: Проверьте что MenuView работает

Запустите приложение и проверьте что нет красного label "Unimplemented".

### Шаг 3: Включите нативный UIMenu

Откройте файл `components/ui/glass/GlassDropdownMenu.tsx` и найдите флаг `USE_NATIVE_MENU` в начале файла.

Измените:

```typescript
// БЫЛО:
const USE_NATIVE_MENU = false; // Custom dropdown (default, works in Expo Go)

// СТАЛО:
const USE_NATIVE_MENU = true; // Native UIMenu ✅
```

### Шаг 4: Перезапустите приложение

```bash
# Если используете Expo
npx expo start

# Если используете bare workflow
npx expo run:ios
```

**Готово!** Теперь используется нативный UIMenu 🎉

---

## 📊 Сравнение решений

| Функция                 | Custom Dropdown (default)              | Native UIMenu (optional) |
| ----------------------- | -------------------------------------- | ------------------------ |
| Работает в Expo Go      | ✅ Да                                  | ❌ Нет                   |
| Требует нативную сборку | ❌ Нет                                 | ✅ Да                    |
| Позиция меню            | Фиксированная (top-right, под хедером) | Точно рядом с кнопкой    |
| Анимация                | React Native Animated                  | Нативная iOS             |
| Иконки в меню           | ✅ Да                                  | ❌ Нет (только текст)    |
| Destructive стиль       | ✅ Красный текст                       | ✅ Красный текст         |
| Disabled items          | ✅ Да                                  | ✅ Да                    |
| Active checkmark        | ✅ Да                                  | ❌ Нет                   |

---

## 🐛 Troubleshooting

### "Unimplemented" red label появляется

**Причина:** Нативный модуль `@react-native-menu/menu` не установлен.

**Решение:**

1. Убедитесь что `USE_NATIVE_MENU = false`
2. Перезапустите приложение

### MenuView не показывается после переключения

**Причина:** Pods не установлены на iOS.

**Решение:**

```bash
cd ios
pod install
cd ..
npx expo run:ios
```

### Custom dropdown не закрывается

**Причина:** Backdrop не реагирует на нажатие.

**Решение:** Убедитесь что `Pressable` в Modal имеет `onPress={onClose}`.

---

## 💡 Рекомендации

- **По умолчанию:** оставляйте `USE_NATIVE_MENU = false` (стабильно работает в Expo Go)
- **Если нужен нативный UIMenu:** включайте `USE_NATIVE_MENU = true` только после dev build с `@react-native-menu/menu`

Примечание: Liquid Glass-стиль триггер-кнопки на iOS 26+ включается автоматически при наличии поддержки (см. `utils/platform.ts`, `CAN_USE_LIQUID_GLASS`).

Важно: динамические iOS цвета используются только для иконки/текста в стеклянных контролах (где это нужно). Само custom меню остаётся белым/чёрным.

---

## 📸 Примеры

### Custom Dropdown (работает сейчас)

```
┌─────────────────────────────┐
│ 🔄 Select         [checkmark]│
├─────────────────────────────┤
│ 🔍 Filter         [active]  │
├─────────────────────────────┤
│ ⊞ Two Columns               │
└─────────────────────────────┘
```

### Native UIMenu (после сборки)

```
iOS native contextual menu
появляется точно рядом с кнопкой
с системными анимациями
```

---

## 🔗 Полезные ссылки

- [expo-glass-effect docs](https://docs.expo.dev/versions/latest/sdk/glass-effect/)
- [@react-native-menu/menu](https://github.com/react-native-menu/menu)
- [Apple UIMenu docs](https://developer.apple.com/documentation/uikit/uimenu)
- [EAS Build](https://docs.expo.dev/build/introduction/)
