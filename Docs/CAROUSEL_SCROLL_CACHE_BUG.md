# 🐛 Bug: Неправильные items в карусели при редактировании образа с 1 категорией

**Дата:** 2025-11-09  
**Критичность:** 🔴 HIGH  
**Статус:** ✅ FIXED

---

## 🎯 Описание проблемы

При редактировании образа с **1 категорией** (например, только footwear) в карусели отображаются **ДРУГИЕ вещи** (не те, которые были сохранены в образе).

При редактировании **полного образа** (3+ категории) всё работает корректно.

---

## 🔍 Root Cause Analysis

### Проблема: Конфликт кэша scroll позиций

**Текущая логика кэша** (`CategorySelectorWithSmooth.tsx:175, 208`):

```typescript
const cacheKey = `${tabType}-${category}-${slotIndex}`;
// Пример: "custom-footwear-0"
```

### 🚨 Проблемный сценарий

#### Образ A (полный):

```typescript
customTabCategories = ['footwear', 'tops', 'bottoms'];
customTabSelectedItems = [shoe1, tshirt1, jeans1];
// Footwear в slot 0
// Кэш: "custom-footwear-0" → scrollIndex = 3 (позиция shoe1)
```

#### Закрываем Образ A, открываем Образ B:

```typescript
customTabCategories = ['footwear']; // Только кроссы!
customTabSelectedItems = [shoe2];
// Footwear тоже в slot 0
// Кэш ключ тот же: "custom-footwear-0" → scrollIndex = 3
```

**Результат:**

- Карусель прокручивается к индексу 3
- Показывает shoe1 вместо shoe2 ❌
- Пользователь видит не те кроссовки!

---

## 📊 Почему полные образы работают?

Когда у образов **разное количество категорий**, они используют **разные slot индексы**:

```typescript
// Образ A: footwear в slot 2
Кэш: "custom-footwear-2" → index 5

// Образ B: footwear в slot 0
Кэш: "custom-footwear-0" → index 2

// Нет конфликта! ✅
```

---

## 🕐 Timing Problem: useEffect не успевает

В `CategorySelectorWithSmooth.tsx` есть useEffect (строки 134-170), который **должен** очищать кэш при изменении selectedItems:

```typescript
useEffect(() => {
  // Detect changed items and clear cache
  if (changedSlots.length > 0) {
    setScrollCache((prev) => {
      const next = { ...prev };
      changedSlots.forEach((slot) => {
        delete next[cacheKey];
      });
      return next;
    });
  }
}, [selectedItems]);
```

**Но порядок событий неправильный:**

1. ✅ `setCurrentOutfit(outfitB)` - устанавливает новые данные
2. ✅ React рендерит `CategorySelectorWithSmooth`
3. ❌ **Читает старый кэш** `custom-footwear-0` = 3 (от Образа A)
4. ❌ Передаёт `initialScrollIndex=3` в `SmoothCarousel`
5. ❌ `SmoothCarousel` инициализируется и **прокручивается к индексу 3**
6. ✅ useEffect срабатывает и очищает кэш
7. 😭 **Но уже поздно!** Карусель проинициализирована с неправильной позицией

---

## ✅ Решение

### Вариант 1: Добавить outfitId в ключ кэша (РЕКОМЕНДУЕТСЯ)

Изолировать кэш для каждого образа:

```typescript
// CategorySelectorWithSmooth.tsx
interface CategorySelectorWithSmoothProps {
  // ... existing props
  outfitId?: string; // ✅ NEW
}

export function CategorySelectorWithSmooth({
  categories,
  wardrobeItems,
  selectedItems,
  tabType,
  onItemSelect,
  outfitId, // ✅ NEW
}: CategorySelectorWithSmoothProps) {
  // Generate cache key with outfit isolation
  const cacheKey = `${outfitId || 'new'}-${tabType}-${category}-${slotIndex}`;
  //                ^^^^^^^^^^^^^^ Уникально для каждого образа!
}
```

**Преимущества:**

- ✅ Полная изоляция кэша между образами
- ✅ Новый образ: `outfitId='new'` - свой кэш
- ✅ Редактирование: `outfitId='uuid'` - свой кэш
- ✅ Каждый образ "помнит" свои позиции скролла

### Вариант 2: Очищать кэш при смене образа

Добавить `useEffect` с зависимостью от `outfitId`:

```typescript
useEffect(() => {
  // Clear all cache when switching outfits
  console.log('🗑️ Clearing scroll cache on outfit change');
  setScrollCache({});
}, [outfitId]);
```

**Недостаток:** Теряется кэш при переключении между табами в рамках одного образа.

---

## 📝 Файлы для изменения

### 1. `CategorySelectorWithSmooth.tsx`

```typescript
// Строка 62-68: Добавить outfitId в props
interface CategorySelectorWithSmoothProps {
  categories: ItemCategory[];
  wardrobeItems: WardrobeItem[];
  selectedItems: (WardrobeItem | null)[];
  tabType: OutfitTabType;
  onItemSelect: (slotIndex: number, item: WardrobeItem | null) => void;
  outfitId?: string;  // ✅ NEW
}

// Строка 74-80: Принять outfitId
export function CategorySelectorWithSmooth({
  categories,
  wardrobeItems,
  selectedItems,
  tabType,
  onItemSelect,
  outfitId,  // ✅ NEW
}: CategorySelectorWithSmoothProps) {

// Строка 175: Обновить генерацию ключа
const cacheKey = `${outfitId || 'new'}-${tabType}-${category}-${slotIndex}`;

// Строка 208: Обновить генерацию ключа
const cacheKey = `${outfitId || 'new'}-${tabType}-${category}-${slotIndex}`;
```

### 2. `ItemSelectionStepNew.tsx`

```typescript
// Строка 24-37: Получить currentOutfit
const {
  currentOutfit,  // ✅ Already exists
  selectedItemsForCreation,
  // ...
} = useOutfitStore();

// Строка 228-234: Передать outfitId в CategorySelector
<CategorySelectorWithSmooth
  categories={currentTabCategories}
  wardrobeItems={wardrobeItems}
  selectedItems={selectedItemsForCreation}
  tabType={activeTab}
  onItemSelect={handleItemSelect}
  outfitId={currentOutfit?.id}  // ✅ NEW - Pass outfit ID for cache isolation
/>
```

---

## 🧪 Тест-кейсы

### Test 1: Образ с 1 категорией

```
1. Создать Образ A: ['footwear'] → выбрать shoe1 → сохранить
2. Создать Образ B: ['footwear'] → выбрать shoe2 → сохранить
3. Открыть Образ A на редактирование
   ✅ Ожидание: В карусели показан shoe1
4. Закрыть, открыть Образ B
   ✅ Ожидание: В карусели показан shoe2 (НЕ shoe1!)
```

### Test 2: Образы с одинаковой категорией в разных позициях

```
1. Образ A: ['footwear', 'tops', 'bottoms'] → shoe1, shirt1, jeans1
2. Образ B: ['tops', 'bottoms', 'footwear'] → shirt2, jeans2, shoe2
3. Открыть A → footwear в slot 0 показывает shoe1 ✅
4. Открыть B → footwear в slot 2 показывает shoe2 ✅
```

### Test 3: Переключение табов в одном образе

```
1. Создать образ с полным набором
2. Открыть Basic tab → проскроллить tops к index 5
3. Переключить на Dress tab
4. Вернуться на Basic tab
   ✅ Ожидание: tops всё ещё на index 5 (кэш сохранён)
```

---

## 🎯 Ключевые изменения

| Было                                          | Стало                                                     |
| --------------------------------------------- | --------------------------------------------------------- |
| `cacheKey = "${tabType}-${category}-${slot}"` | `cacheKey = "${outfitId}-${tabType}-${category}-${slot}"` |
| Кэш общий для всех образов                    | Кэш изолирован по образам                                 |
| Конфликты при одинаковых slot/category        | Нет конфликтов                                            |

---

## 📋 Чеклист

- [x] Добавить `outfitId` в props `CategorySelectorWithSmoothProps`
- [x] Обновить генерацию `cacheKey` в трёх местах (строки 161, 177, 211)
- [x] Передать `currentOutfit?.id` в `ItemSelectionStepNew.tsx`
- [x] Добавить `outfitId` в зависимости useEffect (строка 172)
- [ ] Протестировать Test 1: образ с 1 категорией
- [ ] Протестировать Test 2: разные позиции одной категории
- [ ] Протестировать Test 3: переключение табов
- [ ] Проверить логи в консоли
- [ ] Удалить лишние console.log после исправления

---

## ✅ Что было исправлено

### Изменённые файлы:

#### 1. `CategorySelectorWithSmooth.tsx`

- ✅ Добавлен `outfitId?: string` в interface props (строка 68)
- ✅ Принимается `outfitId` в параметрах (строка 81)
- ✅ Обновлён `cacheKey` в `handleScrollIndexChange`: `${outfitId || 'new'}-...` (строка 177)
- ✅ Обновлён `cacheKey` при очистке кэша в useEffect (строка 161)
- ✅ Обновлён `cacheKey` при lookup кэша (строка 211)
- ✅ Добавлен `outfitId` в зависимости useEffect (строка 172)
- ✅ Добавлен `outfitId` в зависимости handleScrollIndexChange (строка 189)

#### 2. `ItemSelectionStepNew.tsx`

- ✅ Передаётся `outfitId={currentOutfit?.id}` в CategorySelectorWithSmooth (строка 234)

### Результат:

Теперь каждый образ имеет **изолированный кэш scroll позиций**:

- Новый образ: кэш ключ начинается с `new-...`
- Образ с ID 'abc': кэш ключ начинается с `abc-...`
- Образ с ID 'xyz': кэш ключ начинается с `xyz-...`

**Конфликты между образами невозможны!** ✅

---

## 🚀 Следующие шаги

1. **Применить фикс** - добавить `outfitId` в кэш ключ
2. **Тестирование** - пройти все 3 тест-кейса
3. **Мониторинг** - проверить логи на наличие других проблем
4. **Cleanup** - убрать debug логи

---

**Примечание:** Эта проблема связана с проблемами из `OUTFIT_CRITICAL_FIXES_SUMMARY.md`, но является отдельным багом, специфичным для системы кэширования scroll позиций.
