# План: Сохранение ссылки на товар и кнопка "Купить"

**Дата создания:** 21 декабря 2025
**Статус:** ✅ Реализовано + Debug logging добавлен

---

## TL;DR

Функционал **частично реализован**, но не работал из-за проблем в цепочке передачи данных. Добавлен комплексный debug logging для отслеживания flow + визуальный индикатор корзины на карточках вещей.

---

## 📊 Текущее состояние

### ✅ Что уже реализовано:

| Компонент                        | Статус | Файл                                                       |
| -------------------------------- | ------ | ---------------------------------------------------------- |
| Тип `sourceUrl` в ItemMetadata   | ✅     | [types/models/item.ts](../../types/models/item.ts)         |
| Тип `productUrl` в DetectedImage | ✅     | [types/models/store.ts](../../types/models/store.ts)       |
| Тип `sourceUrl` в CartItem       | ✅     | [types/models/store.ts](../../types/models/store.ts)       |
| Кнопка "Открыть в магазине"      | ✅     | [app/item/[id].tsx](../../app/item/%5Bid%5D.tsx#L780-L800) |
| Логика сохранения sourceUrl      | ✅     | [app/add-item.tsx](../../app/add-item.tsx#L436-L465)       |
| DB поле metadata (JSONB)         | ✅     | Таблица `items.metadata`                                   |

### ❌ Что НЕ работает:

1. **Нет вещей с `sourceUrl` в БД** — Запрос `WHERE metadata->>'sourceUrl' IS NOT NULL` возвращает 0 результатов
2. **Нет индикатора на ItemCard** — Пользователь не видит, что вещь можно купить, пока не откроет детали
3. **Неясно, сохраняется ли productUrl** — Нужно проверить скрипт детекции изображений

---

## 🔍 Анализ проблемы

### Цепочка данных (должна работать):

```
WebView (страница товара)
    ↓ [imageDetectionScript.ts]
DetectedImage { url, productUrl? }
    ↓ [GalleryBottomSheet → addToCart]
CartItem { image, sourceUrl, sourceName }
    ↓ [startBatchUpload → add-item.tsx]
CreateItemInput { metadata: { sourceUrl } }
    ↓ [itemServiceOffline.createItem]
DB: items.metadata = { "sourceUrl": "...", "source": "web" }
```

### Возможные точки отказа:

1. **imageDetectionScript.ts** — Функция `findProductUrl()` может не находить ссылки
2. **shoppingBrowserStore.addToCart()** — sourceUrl может не передаваться
3. **add-item.tsx** — Логика batch mode может быть с багом
4. **itemServiceOffline.createItem()** — metadata может не сохраняться в БД

---

## 📝 План имплементации

### Шаг 1: Диагностика и отладка детекции URL

**Файл:** [components/shopping/imageDetectionScript.ts](../../components/shopping/imageDetectionScript.ts)

1. Добавить console.log в функцию `findProductUrl()` для отладки
2. Проверить, что скрипт корректно находит `<a>` теги с product URL
3. Тест на реальных магазинах (Lamoda, Wildberries, ASOS)

**Проверить:**

```javascript
// В imageDetectionScript.ts, функция findProductUrl
console.log('[ProductUrl] Found link:', anchorElement.href);
```

---

### Шаг 2: Проверить сохранение sourceUrl в store

**Файл:** [store/shoppingBrowserStore.ts](../../store/shoppingBrowserStore.ts#L270-L295)

Функция `addToCart` должна сохранять:

- `sourceUrl` — текущий URL страницы в WebView
- `image.productUrl` — URL конкретного товара (если найден)

**Добавить логирование:**

```typescript
addToCart: async (images, sourceUrl, sourceName) => {
  console.log('[Cart] Adding with sourceUrl:', sourceUrl);
  console.log(
    '[Cart] Images with productUrl:',
    images.map((i) => i.productUrl),
  );
  // ...
};
```

---

### Шаг 3: Проверить передачу в add-item.tsx

**Файл:** [app/add-item.tsx](../../app/add-item.tsx#L436-L465)

Текущая логика (строки 436-447):

```typescript
let finalSourceUrl = webImageUrl;
if (isBatchMode) {
  const batchItem = batchQueue[currentBatchIndex];
  if (batchItem?.image?.productUrl) {
    finalSourceUrl = batchItem.image.productUrl;
  } else if (batchItem?.sourceUrl) {
    finalSourceUrl = batchItem.sourceUrl;
  }
}
```

**Проблема:** Если `isBatchMode = false` и пользователь добавляет напрямую, `webImageUrl` может быть URL изображения, а не страницы товара.

**Решение:** Убедиться, что sourceUrl всегда берётся из CartItem или параметров навигации.

---

### Шаг 4: Добавить миграцию БД (опционально)

Текущее решение: `sourceUrl` хранится в `items.metadata` (JSONB).

**Альтернатива:** Создать отдельный столбец для лучшей индексации и запросов.

```sql
-- Опциональная миграция для отдельного столбца
ALTER TABLE items ADD COLUMN source_url TEXT;

-- Индекс для поиска вещей с ссылками
CREATE INDEX idx_items_source_url ON items(source_url) WHERE source_url IS NOT NULL;
```

**Рекомендация:** Оставить в JSONB, если нет требований к SQL-запросам по этому полю.

---

### Шаг 5: Добавить индикатор на ItemCard

**Файл:** [components/wardrobe/ItemCard.tsx](../../components/wardrobe/ItemCard.tsx)

**Добавить:**

1. Пропс `hasSourceUrl?: boolean`
2. Иконку корзины/ссылки в углу карточки

```tsx
interface ItemCardProps {
  item: WardrobeItem;
  // ... existing props
  hasSourceUrl?: boolean; // NEW
}

// В JSX, после favoriteButton:
{
  hasSourceUrl && (
    <View style={styles.webSourceIndicator}>
      <Ionicons name="cart-outline" size={14} color="#007AFF" />
    </View>
  );
}
```

**Стили:**

```typescript
webSourceIndicator: {
  position: 'absolute',
  left: 8,
  top: 8,
  backgroundColor: 'rgba(255, 255, 255, 0.9)',
  borderRadius: 12,
  padding: 4,
}
```

---

### Шаг 6: Передать hasSourceUrl в wardrobe screen

**Файл:** [app/(tabs)/wardrobe.tsx](<../../app/(tabs)/wardrobe.tsx>)

При рендере `ItemCard` передать:

```tsx
<ItemCard
  item={item}
  hasSourceUrl={!!item.metadata?.sourceUrl}
  // ... other props
/>
```

---

### Шаг 7: Улучшить кнопку на детальном экране

**Файл:** [app/item/[id].tsx](../../app/item/%5Bid%5D.tsx#L780-L800)

Текущая реализация хорошая, но можно добавить:

1. Название магазина (если есть в metadata)
2. Иконку магазина
3. Анимацию при нажатии

---

## ✅ Реализация (21 декабря 2025)

### Изменённые файлы:

1. **[utils/shopping/imageDetection.ts](../../utils/shopping/imageDetection.ts)**
   - ✅ Добавлен debug logging в функцию `findProductUrl()` для отслеживания найденных ссылок
   - ✅ Логирование productUrl для каждого детектированного изображения
   - ✅ Улучшена инициализация с проверкой ReactNativeWebView

2. **[store/shoppingBrowserStore.ts](../../store/shoppingBrowserStore.ts)**
   - ✅ Добавлен debug logging в функцию `addToCart()`
   - ✅ Отображение количества изображений с productUrl

3. **[app/add-item.tsx](../../app/add-item.tsx)**
   - ✅ Улучшен debug logging для отслеживания sourceUrl flow
   - ✅ Логирование приоритета: productUrl > sourceUrl > webImageUrl
   - ✅ Вынесена metadata в отдельную переменную для читаемости

4. **[services/wardrobe/itemServiceOffline.ts](../../services/wardrobe/itemServiceOffline.ts)**
   - ✅ Добавлена функция `logItemCreation()` для отладки
   - ✅ Вызов логирования при создании вещи
   - ✅ Отображение metadata с sourceUrl

5. **[components/wardrobe/ItemCard.tsx](../../components/wardrobe/ItemCard.tsx)**
   - ✅ Добавлен пропс `hasSourceUrl?: boolean`
   - ✅ Добавлен визуальный индикатор корзины (cart-outline) в левом верхнем углу
   - ✅ Стили с тенью и прозрачным белым фоном
   - ✅ Индикатор скрывается в режиме выбора (isSelectable)

6. **[components/wardrobe/ItemGrid.tsx](../../components/wardrobe/ItemGrid.tsx)**
   - ✅ Передача `hasSourceUrl={!!item.metadata?.sourceUrl}` в ItemCard

7. **[app/shopping/browser.tsx](../../app/shopping/browser.tsx)** ⚠️ КРИТИЧНЫЙ ФИКС
   - ✅ Исправлена инжекция скрипта детекции при нажатии "Сканировать"
   - ✅ Добавлена полная отладочная информация в консоль
   - ✅ Улучшена обработка ошибок с алертами для пользователя
   - ✅ Добавлен timeout с сообщением если детекция не отвечает

8. **[components/shopping/GalleryBottomSheet.tsx](../../components/shopping/GalleryBottomSheet.tsx)**
   - ✅ Добавлен debug logging при "Добавить сейчас"
   - ✅ Добавлен debug logging при "В корзину"
   - ✅ Отображение sourceUrl и productUrl для каждого изображения

### Что работает:

- ✅ Debug logging на всех этапах flow
- ✅ Исправлена инжекция и вызов скрипта детекции
- ✅ Визуальный индикатор на карточках вещей с sourceUrl
- ✅ Кнопка "Открыть в магазине" на детальном экране (уже была)
- ✅ Алерты с понятными сообщениями об ошибках

### Основные исправления проблемы детекции:

**Проблема:** Скрипт детекции не вызывался правильно при нажатии "Сканировать"

**Решение:**

1. Скрипт теперь **re-inject** при каждом скане для надёжности
2. Добавлена проверка существования `window.__obrazzDetectImages` перед вызовом
3. Добавлена отправка ошибок обратно в React Native если функция не найдена
4. Добавлен debug info в сообщениях об ошибках (hasFunction, hasWebView, url)
5. Улучшена обработка timeout с алертом для пользователя

---

## 🧪 Тестирование

### Сценарий 1: Добавление через браузер

1. Открыть магазин (Lamoda)
2. Перейти на страницу товара
3. Нажать "Scan" → выбрать изображение
4. Добавить в гардероб
5. **Ожидание:** `sourceUrl` сохранён в metadata

### Сценарий 2: Batch добавление из корзины

1. Добавить несколько товаров в корзину браузера
2. Нажать "Add All" → batch upload
3. **Ожидание:** Каждая вещь имеет свой `sourceUrl`

### Сценарий 3: Проверка кнопки "Купить"

1. Открыть вещь с `sourceUrl`
2. Нажать "Открыть в магазине"
3. **Ожидание:** Открывается браузер с URL товара

---

## 📁 Файлы для изменений

| Файл                                          | Тип изменения                 |
| --------------------------------------------- | ----------------------------- |
| `components/shopping/imageDetectionScript.ts` | Debug + улучшение детекции    |
| `store/shoppingBrowserStore.ts`               | Debug logging                 |
| `app/add-item.tsx`                            | Исправление логики sourceUrl  |
| `components/wardrobe/ItemCard.tsx`            | Добавить индикатор            |
| `app/(tabs)/wardrobe.tsx`                     | Передать hasSourceUrl         |
| `services/wardrobe/itemServiceOffline.ts`     | Проверить сохранение metadata |

---

## ⏱️ Оценка времени

| Задача                | Время         |
| --------------------- | ------------- |
| Диагностика и debug   | 1-2 часа      |
| Исправление flow      | 1-2 часа      |
| Индикатор на ItemCard | 30 мин        |
| Тестирование          | 1 час         |
| **Итого**             | **4-5 часов** |

---

## ❓ Открытые вопросы

1. **Нужен ли отдельный столбец `source_url` в БД?**
   - Текущий JSONB подход достаточен для MVP
   - Отдельный столбец нужен только для SQL-запросов/фильтрации

2. **Какие магазины поддерживать?**
   - Текущий скрипт универсальный, но может не работать на всех сайтах
   - Можно добавить специфичные правила для популярных магазинов

3. **Показывать индикатор на всех карточках или только при наведении?**
   - Рекомендация: Маленькая иконка всегда видна

---

## 📚 Связанная документация

- [PRD Obrazz](../PRDobrazz.md) — Product Requirements
- [Implementation](../Implementation.md) — Roadmap
- [Bug Tracking](../Bug_tracking.md) — Known issues
