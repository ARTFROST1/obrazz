# 🔧 Xcode Setup Steps (Historical)

> ⚠️ Этот документ устарел.
> Ранее он описывал ручные шаги в Xcode (добавление Swift файлов, ручное линкование фреймворков).
> Сейчас `subject-lifter` подключается автоматически через Expo Config Plugin (локальный pod) и использует **Apple Vision (`Vision.framework`)**.

**Актуальная документация (source of truth):**

- `Docs/Features/iOS_OnDevice_Background_Removal.md`

Для проверки запуска ориентируйтесь на логи вида:

```
[BackgroundRemover] Apple Vision availability: true
```
