# Shopping Browser — Gallery View & Cart System

> **Дата:** 12 декабря 2025  
> **Версия:** 2.0  
> **Статус:** Планирование  
> **Автор:** Анализ на основе текущего кода + новые требования  
> **Зависимости:** Stage 4.11 Web Capture

---

## 📋 Содержание

1. [Обзор изменений](#-обзор-изменений)
2. [Новый User Flow](#-новый-user-flow)
3. [Архитектура компонентов](#-архитектура-компонентов)
4. [Детальные спецификации](#-детальные-спецификации)
5. [Состояние и хранение данных](#-состояние-и-хранение-данных)
6. [UI/UX Best Practices](#-uiux-best-practices)
7. [План реализации](#-план-реализации)
8. [Технические детали](#-технические-детали)

---

## 🎯 Обзор изменений

### Текущее поведение (Stage 4.11 v1):

```
WebView детектит изображения
    ↓
Bottom Sheet появляется АВТОМАТИЧЕСКИ
    ↓
Одно изображение — одно добавление
```

### Новое поведение (Stage 4.11 v2):

```
WebView детектит изображения В ФОНЕ (без отображения)
    ↓
Кнопка "🔍 Найдено N" внизу экрана
    ↓
Tap → Bottom Sheet с ГАЛЕРЕЕЙ всех найденных вещей
    ↓
Pinterest-style Masonry Layout (разные aspect ratios)
    ↓
Multi-select: tap на изображения для выделения
    ↓
Две inline кнопки:
├── "Добавить сейчас" → обрабатывает ВСЕ из корзины по очереди
└── "В корзину" → добавляет выделенные в корзину
    ↓
Иконка корзины в Top Bar (справа)
    ↓
Cart Screen: список накопленных вещей
```

---

## 🔄 Новый User Flow

### Основной сценарий:

```
┌─────────────────────────────────────┐
│  1. Пользователь открывает магазин  │
│     через Shopping Stories          │
└──────────────┬──────────────────────┘
               ▼
┌─────────────────────────────────────┐
│  2. WebView загружает сайт          │
│     Скрипт детектит изображения     │
│     В ФОНЕ (без UI)                 │
└──────────────┬──────────────────────┘
               ▼
┌─────────────────────────────────────┐
│  3. Кнопка появляется внизу:        │
│     "🔍 Найдено 12 вещей"           │
│     (только если detectedCount > 0) │
└──────────────┬──────────────────────┘
               ▼
┌─────────────────────────────────────┐
│  4. Tap на кнопку → Bottom Sheet    │
│     открывается с галереей          │
└──────────────┬──────────────────────┘
               ▼
┌─────────────────────────────────────┐
│  5. Пользователь выбирает           │
│     понравившиеся вещи (tap)        │
│     Selected items подсвечиваются   │
└──────────────┬──────────────────────┘
               ▼
       ┌───────┴───────┐
       ▼               ▼
┌──────────────┐ ┌──────────────┐
│ "Добавить    │ │ "В корзину"  │
│  сейчас"     │ │              │
└──────┬───────┘ └──────┬───────┘
       │                │
       ▼                ▼
┌──────────────┐ ┌──────────────┐
│ Sequential   │ │ Add to cart  │
│ Add Item     │ │ storage      │
│ process for  │ │ Continue     │
│ ALL cart +   │ │ browsing     │
│ selected     │ │              │
└──────────────┘ └──────────────┘
```

### Cart Flow:

```
┌─────────────────────────────────────┐
│  Иконка корзины 🛒 в Top Bar        │
│  Badge с количеством items          │
└──────────────┬──────────────────────┘
               ▼
┌─────────────────────────────────────┐
│  Cart Screen (Full Screen Modal)    │
│  ├── Список/Grid всех вещей         │
│  ├── Swipe to delete                │
│  └── Кнопка "Добавить всё"          │
└──────────────┬──────────────────────┘
               ▼
┌─────────────────────────────────────┐
│  Sequential processing:             │
│  Item 1 → Add Item Screen → Save    │
│  Item 2 → Add Item Screen → Save    │
│  ...                                │
│  Item N → Add Item Screen → Save    │
└─────────────────────────────────────┘
```

---

## 🏗 Архитектура компонентов

### Текущая структура:

```
components/shopping/
├── ShoppingStoriesCarousel.tsx  # На Home Screen
└── DetectedItemSheet.tsx        # Текущий Bottom Sheet (будет переработан)

app/shopping/
└── browser.tsx                  # WebView экран

store/
└── shoppingBrowserStore.ts      # Zustand store
```

### Новая структура:

```
components/shopping/
├── ShoppingStoriesCarousel.tsx       # Без изменений
├── DetectedItemsGallerySheet.tsx     # НОВЫЙ: Bottom Sheet с галереей
├── MasonryGallery.tsx                # НОВЫЙ: Pinterest-style layout
├── GalleryImageItem.tsx              # НОВЫЙ: Отдельный item в галерее
├── CartButton.tsx                    # НОВЫЙ: Иконка корзины с badge
├── CartSheet.tsx                     # НОВЫЙ: Cart как Bottom Sheet/Modal
├── CartItemRow.tsx                   # НОВЫЙ: Элемент списка в корзине
├── DetectionFAB.tsx                  # НОВЫЙ: Floating кнопка "Найдено N"
└── AddAllProgressModal.tsx           # НОВЫЙ: Прогресс добавления

app/shopping/
├── browser.tsx                       # Обновлённый WebView экран
└── cart.tsx                          # НОВЫЙ: Full-screen Cart (опционально)

store/
└── shoppingBrowserStore.ts           # Расширенный store с cart state

types/models/
└── store.ts                          # Расширенные типы
```

---

## 📐 Детальные спецификации

### 1. Detection FAB (Floating Action Button)

**Расположение:** Низ экрана, по центру  
**Видимость:** Только когда `detectedImages.length > 0`

```
┌─────────────────────────────────────┐
│                                     │
│           [ WebView ]               │
│                                     │
│                                     │
├─────────────────────────────────────┤
│   ┌───────────────────────────┐     │
│   │ 🔍 Найдено 12 вещей       │     │  <- Detection FAB
│   └───────────────────────────┘     │
└─────────────────────────────────────┘
```

**Спецификации:**

```typescript
// DetectionFAB.tsx
{
  height: 48,
  paddingHorizontal: 24,
  borderRadius: 24,
  backgroundColor: '#007AFF',  // iOS Blue
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 4 },
  shadowOpacity: 0.25,
  shadowRadius: 8,
  elevation: 8,

  // Позиционирование
  position: 'absolute',
  bottom: 24,
  alignSelf: 'center',

  // Анимация появления
  animation: 'slideUp + fadeIn',
  duration: 300,
}

// Текст
{
  color: '#FFFFFF',
  fontSize: 15,
  fontWeight: '600',
}
```

**Поведение:**

- Появляется с анимацией при `detectedImages.length > 0`
- Исчезает при `detectedImages.length === 0`
- Tap → открывает `DetectedItemsGallerySheet`
- НЕ показывает автоматически Bottom Sheet

---

### 2. Updated Top Bar с Cart Button

```
┌─────────────────────────────────────┐
│  [✕]  ZARA  H&M  Asos  [🛒²] [⋯]  │
└─────────────────────────────────────┘
                          ↑
                    Cart Button
                    with Badge
```

**CartButton спецификации:**

```typescript
// CartButton.tsx
{
  width: 40,
  height: 40,
  alignItems: 'center',
  justifyContent: 'center',
}

// Badge (если cartItems.length > 0)
{
  position: 'absolute',
  top: 4,
  right: 4,
  minWidth: 18,
  height: 18,
  borderRadius: 9,
  backgroundColor: '#FF3B30',  // Error color
  alignItems: 'center',
  justifyContent: 'center',
}

// Badge text
{
  color: '#FFFFFF',
  fontSize: 11,
  fontWeight: '700',
}
```

**Поведение:**

- Показывается всегда (даже с badge = 0)
- Badge появляется при `cartItems.length > 0`
- Tap → открывает `CartSheet` или навигация на `cart.tsx`

---

### 3. DetectedItemsGallerySheet (Bottom Sheet)

**Layout:**

```
┌─────────────────────────────────────┐
│           ─────  (handle)           │
│                                     │
│  🎯 Найденные вещи          [✕]     │
│  Выбрано: 3 из 12                   │
│                                     │
│  ┌────────────────────────────────┐ │
│  │  MASONRY GALLERY              │ │
│  │  ┌─────┐ ┌─────────┐          │ │
│  │  │ img │ │   img   │ ┌─────┐  │ │
│  │  │ 3:4 │ │   1:1   │ │ img │  │ │
│  │  │ ✓   │ └─────────┘ │ 4:5 │  │ │
│  │  └─────┘ ┌─────┐     │     │  │ │
│  │  ┌─────┐ │ img │     └─────┘  │ │
│  │  │ img │ │ 2:3 │ ┌─────────┐  │ │
│  │  │ 1:1 │ │ ✓   │ │   img   │  │ │
│  │  │     │ └─────┘ │   16:9  │  │ │
│  │  └─────┘         └─────────┘  │ │
│  └────────────────────────────────┘ │
│                                     │
│  ┌──────────────┬──────────────┐    │
│  │ Добавить     │ В корзину 🛒 │    │
│  │ сейчас  ➕   │              │    │
│  └──────────────┴──────────────┘    │
└─────────────────────────────────────┘
```

**Спецификации:**

```typescript
// DetectedItemsGallerySheet.tsx
{
  // Sheet
  maxHeight: '80%',  // SCREEN_HEIGHT * 0.8
  borderTopLeftRadius: 24,
  borderTopRightRadius: 24,
  backgroundColor: '#FFFFFF',

  // Handle
  handle: {
    width: 40,
    height: 4,
    borderRadius: 2,
    backgroundColor: '#C4C4C4',
    marginTop: 12,
    alignSelf: 'center',
  },

  // Header
  header: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingHorizontal: 20,
    paddingTop: 16,
    paddingBottom: 8,
  },

  // Title
  title: {
    fontSize: 18,
    fontWeight: '600',
    color: '#000000',
  },

  // Subtitle (selection count)
  subtitle: {
    fontSize: 13,
    color: '#666666',
    marginTop: 4,
  },
}
```

---

### 4. MasonryGallery Component

**Концепция:** Pinterest-style layout с разными aspect ratios изображений

**Алгоритм расположения:**

1. Два столбца (columns = 2)
2. Каждое изображение добавляется в столбец с меньшей высотой
3. Gap между элементами: 8px
4. Сохраняем оригинальный aspect ratio каждого изображения

```typescript
// MasonryGallery.tsx
interface MasonryGalleryProps {
  images: DetectedImage[];
  selectedIds: Set<string>;
  onImageSelect: (image: DetectedImage) => void;
  columns?: number;
  gap?: number;
}

// Алгоритм расположения (упрощённый)
function calculateMasonryLayout(
  images: DetectedImage[],
  columns: number,
  containerWidth: number,
  gap: number,
): MasonryItem[] {
  const columnWidth = (containerWidth - (columns - 1) * gap) / columns;
  const columnHeights = new Array(columns).fill(0);

  return images.map((image) => {
    // Найти столбец с минимальной высотой
    const shortestColumn = columnHeights.indexOf(Math.min(...columnHeights));

    // Рассчитать размеры
    const aspectRatio = image.width / image.height;
    const itemHeight = columnWidth / aspectRatio;

    const position = {
      x: shortestColumn * (columnWidth + gap),
      y: columnHeights[shortestColumn],
      width: columnWidth,
      height: itemHeight,
    };

    // Обновить высоту столбца
    columnHeights[shortestColumn] += itemHeight + gap;

    return { image, position };
  });
}
```

**Visual пример:**

```
Column 1          Column 2
┌─────────┐       ┌─────────────┐
│         │       │             │
│  3:4    │       │    1:1      │
│         │       │             │
│         │       └─────────────┘
└─────────┘       ┌─────────┐
┌─────────────┐   │         │
│             │   │  4:5    │
│    1:1      │   │         │
│             │   │         │
└─────────────┘   └─────────┘
┌─────────┐       ┌─────────────────┐
│  2:3    │       │      16:9       │
│         │       └─────────────────┘
└─────────┘
```

---

### 5. GalleryImageItem Component

**States:**

- Default: нет выделения
- Selected: overlay + checkmark

```
┌─────────────┐      ┌─────────────┐
│             │      │ ┌─────────┐ │
│   Default   │  →   │ │ ✓       │ │  Selected
│   Image     │      │ │ Selected│ │
│             │      │ │  Overlay│ │
└─────────────┘      │ └─────────┘ │
                     └─────────────┘
```

**Спецификации:**

```typescript
// GalleryImageItem.tsx
{
  // Container
  borderRadius: 12,
  overflow: 'hidden',

  // Image
  width: '100%',
  height: '100%',
  resizeMode: 'cover',

  // Selection overlay
  selectedOverlay: {
    ...StyleSheet.absoluteFillObject,
    backgroundColor: 'rgba(0, 122, 255, 0.3)',  // Blue tint
    borderWidth: 3,
    borderColor: '#007AFF',
    borderRadius: 12,
  },

  // Checkmark
  checkmark: {
    position: 'absolute',
    top: 8,
    right: 8,
    width: 24,
    height: 24,
    borderRadius: 12,
    backgroundColor: '#007AFF',
    alignItems: 'center',
    justifyContent: 'center',
  },

  // Animation on select
  animation: {
    scale: [1, 0.95, 1],
    duration: 150,
  },
}
```

---

### 6. Inline Action Buttons

**Layout:**

```
┌──────────────────────────────────────┐
│  ┌───────────────┬───────────────┐   │
│  │   Добавить    │   В корзину   │   │
│  │   сейчас  ➕  │      🛒       │   │
│  └───────────────┴───────────────┘   │
└──────────────────────────────────────┘
      Left Button      Right Button
      (Primary)        (Secondary)
```

**Спецификации:**

```typescript
// Inline buttons container
{
  flexDirection: 'row',
  paddingHorizontal: 16,
  paddingVertical: 16,
  gap: 12,
  borderTopWidth: 1,
  borderTopColor: '#E5E5E5',
  backgroundColor: '#FFFFFF',
}

// Left button ("Добавить сейчас")
{
  flex: 1,
  height: 52,
  borderRadius: 12,
  backgroundColor: '#000000',
  alignItems: 'center',
  justifyContent: 'center',
  flexDirection: 'row',
  gap: 8,
}

// Left button text
{
  color: '#FFFFFF',
  fontSize: 15,
  fontWeight: '600',
}

// Right button ("В корзину")
{
  flex: 1,
  height: 52,
  borderRadius: 12,
  backgroundColor: '#F5F5F5',
  borderWidth: 1,
  borderColor: '#E5E5E5',
  alignItems: 'center',
  justifyContent: 'center',
  flexDirection: 'row',
  gap: 8,
}

// Right button text
{
  color: '#333333',
  fontSize: 15,
  fontWeight: '500',
}
```

**Поведение кнопок:**

#### "Добавить сейчас" (Left)

1. Собирает ВСЕ items из `cartItems` + `selectedImages`
2. Закрывает Gallery Sheet
3. Показывает `AddAllProgressModal`
4. По очереди для каждого item:
   - Открывает `/add-item` с `imageUrl`
   - Ждёт завершения (save или skip)
   - Переходит к следующему
5. После всех — показывает summary

#### "В корзину" (Right)

1. Добавляет `selectedImages` в `cartItems`
2. Очищает `selectedImages`
3. Показывает toast: "Добавлено N вещей в корзину"
4. Остаётся на Gallery Sheet (можно продолжить выбор)

---

### 7. Cart Sheet / Screen

**Два варианта реализации:**

1. **Bottom Sheet** — быстрый просмотр без ухода с браузера
2. **Full Screen Modal** — для детального управления

**Рекомендую:** Bottom Sheet для MVP, Full Screen позже

**Layout (Bottom Sheet):**

```
┌─────────────────────────────────────┐
│           ─────  (handle)           │
│                                     │
│  🛒 Корзина                   [✕]   │
│  5 вещей для добавления             │
│                                     │
│  ┌────────────────────────────────┐ │
│  │ ┌────┐  Платье ZARA      [🗑️] │ │
│  │ │img │  zara.com              │ │
│  │ └────┘                        │ │
│  ├────────────────────────────────┤ │
│  │ ┌────┐  Джинсы H&M       [🗑️] │ │
│  │ │img │  hm.com                │ │
│  │ └────┘                        │ │
│  ├────────────────────────────────┤ │
│  │ ┌────┐  Кроссовки Nike   [🗑️] │ │
│  │ │img │  nike.com              │ │
│  │ └────┘                        │ │
│  └────────────────────────────────┘ │
│                                     │
│  ┌────────────────────────────────┐ │
│  │      Добавить всё (5)  ➕      │ │
│  └────────────────────────────────┘ │
│                                     │
│  ┌────────────────────────────────┐ │
│  │      Очистить корзину  🗑️     │ │
│  └────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**CartItemRow спецификации:**

```typescript
// CartItemRow.tsx
{
  // Container
  flexDirection: 'row',
  alignItems: 'center',
  paddingVertical: 12,
  paddingHorizontal: 16,
  borderBottomWidth: 1,
  borderBottomColor: '#F0F0F0',
}

// Thumbnail
{
  width: 60,
  height: 60,
  borderRadius: 8,
  backgroundColor: '#F5F5F5',
}

// Info container
{
  flex: 1,
  marginLeft: 12,
}

// Title (auto-generated from URL or category)
{
  fontSize: 15,
  fontWeight: '500',
  color: '#000000',
}

// Source URL
{
  fontSize: 12,
  color: '#666666',
  marginTop: 2,
}

// Delete button
{
  width: 40,
  height: 40,
  alignItems: 'center',
  justifyContent: 'center',
}
```

**Swipe to delete:**

- Используем `react-native-gesture-handler` для swipe
- Swipe left → показывает красную кнопку "Удалить"
- Confirm → удаляет из корзины

---

### 8. AddAllProgressModal

**Показывается во время sequential добавления:**

```
┌─────────────────────────────────────┐
│                                     │
│         Добавление вещей            │
│                                     │
│         ┌──────────────────┐        │
│         │   ███████░░░░░   │        │  Progress bar
│         └──────────────────┘        │
│                                     │
│         Вещь 3 из 5                 │
│                                     │
│         [Отменить]                  │
│                                     │
└─────────────────────────────────────┘
```

**Логика:**

1. Modal появляется при старте batch добавления
2. Каждый item открывает Add Item Screen
3. Пользователь может:
   - Сохранить (Save) → переход к следующему
   - Пропустить (Skip) → переход к следующему
   - Отменить всё (Cancel) → закрыть modal
4. После последнего item — summary toast

---

## 💾 Состояние и хранение данных

### Расширенный Store

```typescript
// store/shoppingBrowserStore.ts

interface CartItem {
  id: string; // Уникальный ID
  image: DetectedImage; // Данные изображения
  sourceUrl: string; // URL страницы, где найдено
  sourceName: string; // Название магазина
  addedAt: number; // Timestamp добавления
}

interface ShoppingBrowserState {
  // Existing state...
  stores: Store[];
  loadingStores: boolean;
  tabs: BrowserTab[];
  activeTabId: string | null;
  detectedImages: DetectedImage[];

  // NEW: Selection state
  selectedImageIds: Set<string>; // IDs выбранных в галерее

  // NEW: Cart state
  cartItems: CartItem[];
  showCartSheet: boolean;

  // NEW: Gallery sheet state
  showGallerySheet: boolean;

  // NEW: Batch processing state
  isBatchProcessing: boolean;
  batchProgress: {
    current: number;
    total: number;
  };

  // Existing actions...

  // NEW: Selection actions
  toggleImageSelection: (imageId: string) => void;
  selectAllImages: () => void;
  clearSelection: () => void;

  // NEW: Cart actions
  addToCart: (images: DetectedImage[]) => void;
  removeFromCart: (itemId: string) => void;
  clearCart: () => void;
  showCart: (show: boolean) => void;

  // NEW: Gallery sheet actions
  showGallery: (show: boolean) => void;

  // NEW: Batch processing actions
  startBatchProcessing: (items: CartItem[]) => void;
  updateBatchProgress: (current: number) => void;
  finishBatchProcessing: () => void;
}
```

### AsyncStorage Keys

```typescript
// Корзина сохраняется между сессиями
const CART_KEY = '@shopping_browser_cart';

// При открытии браузера — загрузить cart
// При закрытии — сохранить cart
// При добавлении в cart — сохранить сразу
```

### Типы данных

```typescript
// types/models/store.ts (расширение)

export interface DetectedImage {
  id: string; // НОВОЕ: Уникальный ID (hash URL + dimensions)
  url: string;
  width: number;
  height: number;
  alt?: string;
  category?: string;
  confidence?: number;
}

export interface CartItem {
  id: string;
  image: DetectedImage;
  sourceUrl: string;
  sourceName: string;
  addedAt: number;
}

export interface BatchProcessingState {
  isActive: boolean;
  items: CartItem[];
  currentIndex: number;
  completedCount: number;
  skippedCount: number;
}
```

---

## 🎨 UI/UX Best Practices

### 1. Micro-interactions

```typescript
// Выбор изображения
- Tap: scale(0.95) → scale(1) с spring animation
- Haptic feedback: selection (iOS), vibrate 10ms (Android)

// Добавление в корзину
- Button: scale(0.98) → scale(1)
- Toast появляется снизу с slide up animation
- Badge на cart icon: bounce animation при увеличении

// Открытие Gallery Sheet
- Spring animation с damping ratio 0.8
- Overlay fade in 200ms
```

### 2. Empty States

**Пустая галерея:**

```
┌─────────────────────────────────────┐
│                                     │
│              📷                     │
│                                     │
│     Вещи не найдены на этой         │
│     странице. Попробуйте            │
│     открыть карточку товара.        │
│                                     │
└─────────────────────────────────────┘
```

**Пустая корзина:**

```
┌─────────────────────────────────────┐
│                                     │
│              🛒                     │
│                                     │
│     Корзина пуста                   │
│                                     │
│     Добавьте вещи из магазинов,     │
│     чтобы добавить их в гардероб    │
│     позже.                          │
│                                     │
└─────────────────────────────────────┘
```

### 3. Error Handling

```typescript
// Ошибка загрузки изображения в галерее
- Показать placeholder с иконкой ⚠️
- При tap: "Не удалось загрузить изображение"

// Ошибка добавления в гардероб (batch)
- Показать в summary: "Добавлено 3, ошибок 2"
- Кнопка "Повторить для ошибок"

// Network error
- Toast: "Проверьте подключение к интернету"
- Retry button
```

### 4. Accessibility

```typescript
// VoiceOver / TalkBack
- Gallery images: "Изображение N из M, не выбрано" / "выбрано"
- Cart badge: "Корзина, N элементов"
- Buttons: Clear labels

// Contrast
- Все тексты соответствуют WCAG AA (4.5:1 минимум)

// Touch targets
- Минимум 44x44px для всех интерактивных элементов
```

### 5. Performance

```typescript
// Masonry Gallery
- Использовать FlatList с getItemLayout для virtualization
- Lazy load images с placeholder
- Ограничить до 50 изображений в галерее (pagination если больше)

// Image optimization
- Использовать thumbnail URLs если доступны
- Cache images с react-native-fast-image (опционально)

// Animations
- Все анимации на native driver (useNativeDriver: true)
```

---

## 📅 План реализации

### Phase 1: Core Gallery (3-4 дня)

**День 1-2: Store & Types**

- [ ] Расширить `DetectedImage` с `id` field
- [ ] Добавить selection state в store
- [ ] Добавить cart state в store
- [ ] Создать типы для `CartItem`

**День 3-4: Components**

- [ ] Создать `DetectionFAB.tsx`
- [ ] Создать `MasonryGallery.tsx` (базовая версия)
- [ ] Создать `GalleryImageItem.tsx` с selection
- [ ] Обновить `browser.tsx` — убрать auto-show sheet

### Phase 2: Gallery Sheet (2-3 дня)

**День 5-6:**

- [ ] Создать `DetectedItemsGallerySheet.tsx`
- [ ] Интегрировать `MasonryGallery` в sheet
- [ ] Добавить inline action buttons
- [ ] Selection counter в header

**День 7:**

- [ ] Polish анимаций
- [ ] Haptic feedback
- [ ] Empty state

### Phase 3: Cart System (3-4 дня)

**День 8-9:**

- [ ] Создать `CartButton.tsx` с badge
- [ ] Добавить в Top Bar
- [ ] Создать `CartSheet.tsx`
- [ ] Создать `CartItemRow.tsx` с swipe-to-delete

**День 10-11:**

- [ ] AsyncStorage persistence для cart
- [ ] "Добавить всё" логика
- [ ] Clear cart функционал

### Phase 4: Batch Processing (2-3 дня)

**День 12-13:**

- [ ] Создать `AddAllProgressModal.tsx`
- [ ] Sequential navigation к `/add-item`
- [ ] Handle save/skip/cancel
- [ ] Progress tracking

**День 14:**

- [ ] Summary toast после batch
- [ ] Error handling для batch

### Phase 5: Polish & Testing (2-3 дня)

**День 15-16:**

- [ ] Полный E2E flow testing
- [ ] Performance optimization
- [ ] Edge cases (empty states, errors)
- [ ] Accessibility review

**День 17:**

- [ ] Code cleanup
- [ ] Documentation update
- [ ] PR review

**Общая оценка: 12-17 дней**

---

## 🔧 Технические детали

### Dependencies (уже установлены)

```json
{
  "react-native-gesture-handler": "2.28.0",
  "react-native-reanimated": "4.1.1",
  "react-native-webview": "используется",
  "@react-native-async-storage/async-storage": "2.1.0"
}
```

### Возможные дополнительные

```json
{
  "@gorhom/bottom-sheet": "^4.5.1", // Для улучшенного Bottom Sheet
  "react-native-fast-image": "^8.6.3" // Для кэширования изображений
}
```

### Masonry Layout Implementation

**Вариант 1: Кастомный (рекомендуется)**

```typescript
// Простой двухколоночный masonry с FlatList
// Каждый item рендерится в соответствии с aspect ratio
// Не требует дополнительных зависимостей
```

**Вариант 2: Библиотека**

```json
{
  "@react-native-seoul/masonry-list": "^1.4.2"
}
```

### File Structure After Implementation

```
components/shopping/
├── ShoppingStoriesCarousel.tsx      # Существует
├── DetectedItemSheet.tsx            # DEPRECATED (заменён на Gallery)
├── DetectedItemsGallerySheet.tsx    # НОВЫЙ
├── MasonryGallery.tsx               # НОВЫЙ
├── GalleryImageItem.tsx             # НОВЫЙ
├── CartButton.tsx                   # НОВЫЙ
├── CartSheet.tsx                    # НОВЫЙ
├── CartItemRow.tsx                  # НОВЫЙ
├── DetectionFAB.tsx                 # НОВЫЙ
└── AddAllProgressModal.tsx          # НОВЫЙ

store/
└── shoppingBrowserStore.ts          # Расширенный

types/models/
└── store.ts                         # Расширенный

app/shopping/
└── browser.tsx                      # Обновлённый
```

---

## 📝 Checklist перед реализацией

### Подготовка

- [ ] Review текущего `shoppingBrowserStore.ts`
- [ ] Review текущего `browser.tsx`
- [ ] Убедиться что WebView injection работает
- [ ] Протестировать детекцию на 3+ магазинах

### Design Tokens

- [ ] Подтвердить цвета из UI_UX_doc.md
- [ ] Подтвердить spacing (8px gap, 16px padding)
- [ ] Подтвердить border-radius (12px items, 24px sheets)

### Acceptance Criteria

- [ ] Детекция работает в фоне без auto-show
- [ ] FAB показывает количество найденных
- [ ] Gallery отображает все изображения в masonry
- [ ] Multi-select работает корректно
- [ ] "В корзину" добавляет выбранные
- [ ] Cart сохраняется между сессиями
- [ ] "Добавить сейчас" обрабатывает всё по очереди
- [ ] Progress modal отображает прогресс
- [ ] Все анимации плавные (60 FPS)

---

## 🎯 Success Metrics

1. **UX Improvement:**
   - Пользователь может выбрать несколько вещей за один browse session
   - Cart позволяет накапливать вещи из разных магазинов
   - Batch добавление экономит время

2. **Performance:**
   - Gallery рендерится < 100ms для 30 items
   - Анимации 60 FPS
   - Cart load < 50ms

3. **Reliability:**
   - Cart persists после crash/restart
   - Batch processing handles errors gracefully
   - No memory leaks при долгом browsing

---

> **Следующий шаг:** Начать с Phase 1 — расширение store и создание базовых компонентов.
