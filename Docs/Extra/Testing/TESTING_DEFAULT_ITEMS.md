# 🔧 Инструкции по тестированию системы дефолтных вещей

**Дата:** 17 декабря 2025  
**Обновлено:** После миграции v2

---

## Шаг 1: Очистка кеша и перезапуск

```powershell
# В PowerShell в корневой директории проекта

# 1. Остановите Expo сервер (Ctrl+C)

# 2. Удалите .expo кеш
Remove-Item -Path ".expo" -Recurse -Force -ErrorAction SilentlyContinue

# 3. Очистите Metro bundler cache
npx expo start --clear
```

## Шаг 2: Тестирование для НОВОГО пользователя

### Регистрация нового пользователя

1. **В приложении:**
   - Перейдите на экран регистрации
   - Зарегистрируйте нового пользователя (используйте новый email)
   - Подтвердите email если требуется
2. **Ожидаемый результат:**
   - После успешной регистрации автоматически создаётся профиль
   - Триггер `trigger_copy_default_items` копирует 20 дефолтных вещей
   - Вещи доступны сразу при входе в "Гардероб"

### Проверка в консоли

Смотрите логи в Expo Dev Tools / Metro:

```
[ItemService.getUserItems] Fetching items for user: <uuid>
[ItemService.getUserItems] Fetched items count: 20
[ItemService.getUserItems] Sample item: {
  id: ...,
  name: "...",
  user_id: <uuid>,
  is_default: false
}
[ItemService.getUserItems] Returning 20 items
```

**✅ Успех:** Пользователь видит 20 вещей  
**❌ Ошибка:** Пользователь видит 0 вещей

### Если новый пользователь не видит вещи

```sql
-- Выполните в Supabase SQL Editor:

-- 1. Проверьте, создан ли профиль
SELECT id, email, created_at FROM profiles
WHERE email = 'your_new_user@email.com';

-- 2. Проверьте, есть ли у пользователя вещи
SELECT COUNT(*) as item_count
FROM items
WHERE user_id = '<UUID_FROM_STEP_1>';

-- 3. Если 0, вручную запустите копирование:
INSERT INTO items (
  user_id, name, category, image_url, color,
  colors, primary_color, style, season, tags,
  favorite, is_default, metadata, created_at, updated_at
)
SELECT
  '<UUID_FROM_STEP_1>', -- Замените на реальный UUID
  name, category, image_url, color,
  colors, primary_color, style, season, tags,
  false, false, metadata, NOW(), NOW()
FROM items
WHERE user_id IS NULL AND is_default = true;
```

## Шаг 3: Тестирование для СУЩЕСТВУЮЩИХ пользователей

### Старые пользователи (зарегистрированы ДО миграции)

**Ситуация:** У них могут быть дубликаты или старые дефолтные вещи.

**Решение:**

1. **Выйдите из аккаунта** и войдите заново
2. **Перезагрузите приложение** (полный перезапуск)
3. Откройте "Гардероб"

**Ожидаемый результат:**

- Видны только их личные вещи (без дубликатов)
- Старые "shared" дефолтные вещи больше не показываются

### Если возникают ошибки PGRST116

Это означает, что в AsyncStorage или навигации остались ссылки на удалённые вещи.

**Решение:**

```typescript
// Временно добавьте в useEffect в app/_layout.tsx:

import AsyncStorage from '@react-native-async-storage/async-storage';

useEffect(() => {
  // Очистить все кеши (используйте один раз!)
  AsyncStorage.clear().then(() => {
    console.log('✅ AsyncStorage cleared');
  });
}, []);
```

После очистки перезапустите приложение.

## Шаг 4: Проверка функционала

### Добавление вещи

1. Нажмите FAB "+"
2. Выберите фото
3. Заполните поля
4. Сохраните

**Ожидаемое поведение:**

- Вещь добавляется в гардероб
- `is_default = false` (личная вещь)
- `user_id = <ваш_uuid>`

### Удаление вещей

1. Включите режим выбора (верхний правый угол)
2. Выберите несколько вещей
3. Нажмите "Удалить"

**Ожидаемое поведение:**

- ВСЕ вещи удаляются одинаково (нет разделения на builtin/user)
- После удаления вещи исчезают из гардероба
- Можно удалить даже скопированные дефолтные вещи

### Редактирование вещи

1. Откройте любую вещь
2. Измените название/категорию
3. Сохраните

**Ожидаемое поведение:**

- Изменения сохраняются
- Вещь остаётся видимой в гардеробе

## Шаг 5: SQL-проверка целостности данных

```sql
-- Запустите в Supabase SQL Editor:

-- 1. Проверка системных дефолтных вещей
SELECT
  'System defaults (должно быть 20)' as check_name,
  COUNT(*) as count
FROM items
WHERE is_default = true AND user_id IS NULL

UNION ALL

-- 2. Проверка "плохих" дефолтных вещей
SELECT
  'Bad defaults (должно быть 0)' as check_name,
  COUNT(*) as count
FROM items
WHERE is_default = true AND user_id IS NOT NULL

UNION ALL

-- 3. Статистика по пользователям
SELECT
  'Users with items' as check_name,
  COUNT(DISTINCT user_id) as count
FROM items
WHERE user_id IS NOT NULL;

-- 4. Детальная статистика по пользователям
SELECT
  p.email,
  COUNT(i.id) as total_items,
  SUM(CASE WHEN i.is_default THEN 1 ELSE 0 END) as wrong_defaults,
  MIN(i.created_at) as oldest_item,
  MAX(i.created_at) as newest_item
FROM profiles p
LEFT JOIN items i ON i.user_id = p.id
GROUP BY p.id, p.email
ORDER BY total_items DESC;
```

**Ожидаемые результаты:**

- ✅ System defaults: 20
- ✅ Bad defaults: 0
- ✅ wrong_defaults: 0 для каждого пользователя

## Шаг 6: Проверка RLS политик

```sql
-- Проверьте политики безопасности:

SELECT
  policyname,
  cmd,
  qual
FROM pg_policies
WHERE tablename = 'items'
ORDER BY cmd, policyname;
```

**Ожидаемые политики:**

1. **SELECT:** `Users can view own items and defaults`

   ```sql
   (auth.uid() = user_id) OR
   (is_default = true AND user_id IS NULL)
   ```

2. **INSERT:** `Users can insert own items`

   ```sql
   auth.uid() = user_id AND
   (is_default = false OR is_default IS NULL)
   ```

3. **UPDATE:** `Users can update own items`

   ```sql
   auth.uid() = user_id
   ```

4. **DELETE:** `Users can delete own items`
   ```sql
   auth.uid() = user_id
   ```

## Возможные проблемы и решения

### Проблема: "Cannot coerce the result to a single JSON object"

**Причина:** Попытка загрузить вещь, которая была удалена или недоступна.

**Решение:**

1. Очистите AsyncStorage (см. Шаг 3)
2. Перезапустите приложение
3. Проверьте логи - теперь видно какой ID вызывает ошибку

### Проблема: "InternalBytecode.js not found"

**Причина:** Metro bundler cache corruption.

**Решение:**

```powershell
npx expo start --clear
```

### Проблема: Новый пользователь не видит дефолтные вещи

**Причины:**

1. Триггер не сработал
2. RLS блокирует доступ
3. Auth токен не валиден

**Диагностика:**

```sql
-- Проверьте, что триггер существует
SELECT * FROM information_schema.triggers
WHERE trigger_name = 'trigger_copy_default_items';

-- Проверьте, что функция существует
SELECT proname, prosrc FROM pg_proc
WHERE proname = 'copy_default_items_to_new_user';

-- Вручную запустите копирование (см. Шаг 2)
```

### Проблема: Дубликаты вещей

**Причина:** Старые данные до миграции.

**Решение:**

```sql
-- Удалите дубликаты для конкретного пользователя
WITH duplicates AS (
  SELECT id, ROW_NUMBER() OVER (
    PARTITION BY user_id, name, category
    ORDER BY created_at ASC
  ) as rn
  FROM items
  WHERE user_id = '<YOUR_USER_ID>'
)
DELETE FROM items
WHERE id IN (
  SELECT id FROM duplicates WHERE rn > 1
);
```

## Контрольный чеклист

- [ ] Expo сервер запущен с `--clear`
- [ ] Новый пользователь видит 20 вещей после регистрации
- [ ] Существующий пользователь видит свои вещи
- [ ] Добавление новой вещи работает
- [ ] Удаление вещей работает
- [ ] Редактирование вещей работает
- [ ] Нет ошибок в консоли
- [ ] SQL проверки показывают правильные результаты
- [ ] RLS политики настроены корректно

---

**При возникновении проблем:** Отправьте логи из консоли Expo Dev Tools и результаты SQL-запросов.
