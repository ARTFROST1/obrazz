# ✅ Чеклист перед сборкой APK

## 📋 Обязательные проверки

### 1. Environment Variables (обязательно!)

- [ ] Настроены secrets в EAS (см. [QUICK_BUILD.md](QUICK_BUILD.md))
- [ ] Либо файл `.env` заполнен корректными значениями для локальной сборки

### 2. EAS CLI

- [ ] EAS CLI установлен: `npm install -g eas-cli`
- [ ] Авторизованы в Expo: `eas whoami`
- [ ] Если нет - выполните: `eas login`

### 3. Проверка конфигурации

- [ ] Версия в `app.json` и `app.config.js`: **1.0.0**
- [ ] versionCode в Android: **1**
- [ ] Package name: **com.artfrost.obrazz**
- [ ] Project ID в EAS: **9d046cfa-467c-49e4-9098-9c864d1295c6**

### 4. Зависимости

- [ ] Все пакеты установлены: `npm install`
- [ ] Нет критических ошибок: `npm run type-check`

## 🚀 Запуск сборки

### Вариант A: Облачная сборка (проще)

```bash
eas build --profile preview --platform android
```

- Не требует Android SDK
- Займет ~10-15 минут
- Получите ссылку на скачивание APK

### Вариант B: Локальная сборка (быстрее для опытных)

```bash
eas build --profile preview --platform android --local
```

- Требует Android SDK и настроенное окружение
- Займет ~5-10 минут
- APK будет сохранен локально

## 📱 После успешной сборки

### Установка APK

1. Скачайте APK (облачная) или найдите в папке сборки (локальная)
2. Перенесите на Android устройство
3. Разрешите установку из неизвестных источников
4. Установите APK

### Базовое тестирование

- [ ] Приложение запускается
- [ ] Регистрация/вход работает (Supabase подключен)
- [ ] Камера работает
- [ ] Выбор из галереи работает
- [ ] Добавление вещи с удалением фона (Pixian API)
- [ ] Создание образа работает
- [ ] Сохранение образа в галерею

## ⚠️ Если что-то пошло не так

### Ошибка сборки

1. Проверьте логи в EAS Dashboard: https://expo.dev/accounts/artfrost/projects/obrazz/builds
2. Убедитесь, что все secrets настроены
3. Попробуйте очистить кэш: `npx expo start --clear`

### APK не устанавливается

- Удалите предыдущую версию приложения
- Проверьте, что разрешена установка из неизвестных источников
- Убедитесь, что APK полностью скачан (проверьте размер ~50-80 MB)

### Приложение вылетает при запуске

- Проверьте логи через `adb logcat`
- Убедитесь, что все environment variables настроены
- Проверьте, что Supabase URL и ключи корректны

## 📊 Мониторинг сборки

### Проверить статус

```bash
eas build:list
```

### Просмотреть логи последней сборки

```bash
eas build:view
```

### Отменить текущую сборку

```bash
eas build:cancel
```

## 🎯 Готово к сборке?

Если все пункты выше отмечены ✅, можете запускать:

```bash
# Рекомендуемая команда для первой сборки
eas build --profile preview --platform android
```

---

**Полная документация:** [BUILD_INSTRUCTIONS.md](BUILD_INSTRUCTIONS.md)  
**Быстрые команды:** [QUICK_BUILD.md](QUICK_BUILD.md)
