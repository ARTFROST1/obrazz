# 🎯 Финальная инструкция для сборки APK

## ✅ Текущий статус

- ✅ **EAS CLI установлен** (версия устарела, но работает)
- ✅ **Авторизация:** artfrost
- ✅ **Конфигурация:** app.config.js, app.json, eas.json настроены
- ✅ **versionCode:** 1
- ✅ **Package name:** com.artfrost.obrazz

## 🔧 Рекомендуется перед сборкой

### 1. Обновите EAS CLI (опционально, но рекомендуется)

```bash
npm install -g eas-cli
```

### 2. Проверьте environment variables

```bash
eas env:list
```

Если список пустой или нет обязательных переменных, добавьте их:

```bash
# Для production окружения
eas env:create --name EXPO_PUBLIC_SUPABASE_URL --value "your_value" --environment production
eas env:create --name EXPO_PUBLIC_SUPABASE_ANON_KEY --value "your_value" --environment production
eas env:create --name EXPO_PUBLIC_PIXIAN_API_ID --value "your_value" --environment production
eas env:create --name EXPO_PUBLIC_PIXIAN_API_SECRET --value "your_value" --environment production
```

Или добавьте через веб-интерфейс:
https://expo.dev/accounts/artfrost/projects/obrazz/environment-variables

## 🚀 Запуск сборки

### Вариант 1: Облачная сборка (рекомендуется)

```bash
cd c:\Users\moroz\Desktop\AiWardrobe\obrazz
eas build --profile preview --platform android
```

**Процесс:**

1. EAS загрузит код на серверы
2. Запустится сборка (~10-15 минут)
3. Вы получите ссылку на скачивание APK
4. APK будет доступен в: https://expo.dev/accounts/artfrost/projects/obrazz/builds

### Вариант 2: Локальная сборка (быстрее, требует Android SDK)

```bash
cd c:\Users\moroz\Desktop\AiWardrobe\obrazz
eas build --profile preview --platform android --local
```

**Требования:**

- Android Studio установлен
- Android SDK настроен
- Java JDK установлен
- Переменные окружения настроены

## ⚠️ Важные моменты

### Environment Variables

**Критично!** Без настроенных environment variables приложение не будет работать.

Проверьте, что следующие переменные настроены:

- EXPO_PUBLIC_SUPABASE_URL
- EXPO_PUBLIC_SUPABASE_ANON_KEY
- EXPO_PUBLIC_PIXIAN_API_ID
- EXPO_PUBLIC_PIXIAN_API_SECRET

### Первая сборка

При первой сборке EAS может спросить:

- Хотите ли вы сгенерировать новый keystore? → **Yes**
- Хотите ли вы настроить push notifications? → **No** (если не настроены)

## 📱 После сборки

### Скачивание APK

```bash
# Посмотреть список сборок
eas build:list

# Открыть последнюю сборку в браузере
eas build:view
```

Или просто перейдите:
https://expo.dev/accounts/artfrost/projects/obrazz/builds

### Установка на устройство

1. Скачайте APK по ссылке из EAS
2. Перенесите на Android устройство (ADB, USB, облако)
3. Включите "Установка из неизвестных источников"
4. Установите APK

### Быстрая установка через ADB (если устройство подключено)

```bash
# Скачайте APK, затем:
adb install путь_к_скачанному_apk.apk
```

## 🔍 Мониторинг сборки

### В терминале

Сборка покажет прогресс в реальном времени:

```
✔ Build complete!
📱 APK: https://expo.dev/artifacts/...
```

### В веб-интерфейсе

Откройте: https://expo.dev/accounts/artfrost/projects/obrazz/builds

Здесь вы увидите:

- Статус сборки (queued → building → finished)
- Логи сборки
- Ссылку на скачивание APK
- Размер APK
- Время сборки

## 🐛 Решение проблем

### "Build failed" с ошибкой Gradle

- Проверьте версии в package.json
- Убедитесь, что все зависимости установлены: `npm install`
- Попробуйте очистить кэш: `npx expo start --clear`

### "Environment variables not found"

- Настройте через `eas env:create` или веб-интерфейс
- Убедитесь, что переменные добавлены для нужного окружения

### "APK не устанавливается"

- Удалите старую версию приложения
- Проверьте, что APK полностью скачан
- Разрешите установку из неизвестных источников

### "Приложение вылетает"

- Проверьте логи: `adb logcat | grep obrazz`
- Убедитесь, что environment variables настроены
- Проверьте, что Supabase credentials корректны

## 📊 Ожидаемые результаты

- **Время сборки (облачная):** 10-15 минут
- **Время сборки (локальная):** 5-10 минут
- **Размер APK:** ~50-80 MB
- **Min Android версия:** API 23 (Android 6.0)
- **Target Android версия:** API 34 (Android 14)

## ✨ Готово!

Выполните команду и следуйте инструкциям:

```bash
cd c:\Users\moroz\Desktop\AiWardrobe\obrazz
eas build --profile preview --platform android
```

**Дополнительная документация:**

- [BUILD_INSTRUCTIONS.md](BUILD_INSTRUCTIONS.md) - Полное руководство
- [QUICK_BUILD.md](QUICK_BUILD.md) - Быстрые команды
- [BUILD_CHECKLIST.md](BUILD_CHECKLIST.md) - Чеклист проверки
- [BUILD_READY.md](BUILD_READY.md) - Итоговый отчет

Удачной сборки! 🚀
