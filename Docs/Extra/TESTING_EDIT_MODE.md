# Тестирование Edit Mode - Инструкция

## Как проверить что всё работает правильно

### 1. Откройте DevTools/Console

- В Expo Go: встряхните телефон → Debug Remote JS
- В браузере: откройте Developer Tools → Console

### 2. Последовательность действий

#### A) Создайте новый образ:

1. Создайте образ с элементами в некоторых категориях (например: headwear, tops, bottoms)
2. Оставьте другие категории пустыми (деактивируйте флагом)
3. Сохраните образ

#### B) Откройте образ в edit mode:

1. Откройте детальную страницу образа
2. Нажмите кнопку "Edit Outfit"
3. **Смотрите в консоль!**

### 3. Что вы должны увидеть в консоли

```
🔍 [outfitStore] setCurrentOutfit: {
  outfitId: "xxx-xxx-xxx",
  itemsCount: 3,
  selectedCategories: [
    { category: "headwear", itemId: "item-1" },
    { category: "tops", itemId: "item-2" },
    { category: "bottoms", itemId: "item-3" }
  ]
}

🔍 [ItemSelectionStepNew] Initializing active categories: {
  selectedItems: {
    headwear: "item-1",
    tops: "item-2",
    bottoms: "item-3",
    footwear: null,
    accessories: null,
    ...
  },
  activeCategories: ["headwear", "tops", "bottoms"],
  isEditMode: true
}

🔍 [CategorySelector] Initial scroll for headwear: {
  selectedItemId: "item-1",
  foundAtIndex: 2,
  totalItems: 10
}

🔍 [SmoothCarousel] Initializing headwear: {
  initialScrollIndex: 2,
  calculatedIndex: 17, // с учетом offset для infinite loop
  itemsCount: 10,
  selectedItemId: "item-1"
}
```

### 4. Что проверить визуально

✅ **Активные категории** (те что были заполнены при создании):

- Карусель показывает выбранный элемент
- Флаг заполненный (черный)
- Элементы яркие (opacity: 1)

✅ **Неактивные категории** (те что были пустыми):

- Карусель на первом элементе
- Флаг пустой (серый outline)
- Элементы полупрозрачные (opacity: 0.4)

### 5. Ожидаемое поведение

**Правильно:**

- Карусели в edit mode показывают ТЕ ЖЕ элементы, что были при создании
- Активные/неактивные категории совпадают с оригинальным образом
- Никакого flickering или скачков

**Неправильно (если вы видите это - сообщите):**

- Все карусели на первом элементе
- Все категории активны (хотя образ был создан с пустыми категориями)
- Категории активны, но карусели показывают не те элементы

## Текущая реализация

### Логика активности категорий:

```typescript
// Категория активна = в ней есть выбранный элемент
activeCategories = Set<Category> где selectedItemsForCreation[category] !== null
```

### Логика начальной прокрутки:

```typescript
// Карусель прокручивается к выбранному элементу
initialScrollIndex = items.findIndex((item) => item.id === selectedItemId);
```

### Логика флагов:

```typescript
// Флаг показывает активность категории
<Ionicons
  name={isCategoryActive ? 'flag' : 'flag-outline'}
  color={isCategoryActive ? '#000' : '#999'}
/>
```

## Если что-то не работает

1. Скопируйте ВСЕ логи из консоли
2. Сделайте скриншоты экрана
3. Опишите что ожидали VS что получили
4. Сообщите подробности
