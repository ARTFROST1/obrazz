# 🚀 Быстрый Старт - Apple Vision Background Removal

## ✅ Что уже сделано автоматически

1. ✅ Создан Expo Config Plugin для полной автоматизации
2. ✅ `SubjectLifter` подключается как локальный Pod (Expo Modules)
3. ✅ `Vision.framework` автоматически линкуется
4. ✅ iOS deployment target установлен на 16.0

## 📱 Сейчас идёт сборка

Команда `npx expo run:ios` уже запущена и компилирует проект.

### Что происходит:

```bash
› Compiling образz Pods/ReactCodegen » ComponentDescriptors.cpp
# ... компиляция продолжается ...
```

## ⏱️ Дождитесь завершения сборки

Сборка занимает **5-10 минут** в первый раз.

Когда увидите:

```
✔ Built the app
› Opening on iPhone 17 Pro
```

Приложение автоматически запустится на симуляторе.

> ⚠️ Важно: on-device сегментация через Apple Vision может быть недоступна/нестабильна на iOS Simulator.
> Для финальной проверки качества используйте реальное устройство.

## 🧪 Как протестировать

### 1. После запуска приложения проверьте логи

Должны увидеть в терминале:

```
LOG  [SubjectLifter] Attempting to load native module...
LOG  [SubjectLifter] Native module loaded successfully!  ← ✅ ВАЖНО
LOG  [BackgroundRemover] Apple Vision availability: true ← ✅ ВАЖНО
```

Если видите это - **всё работает!**

### 2. Откройте экран добавления вещи

- Tap на "Wardrobe" tab
- Tap на кнопку "Add Item" (+)
- Выберите фото одежды

### 3. Удалите фон

- Tap на кнопку "Remove Background"
- Должны увидеть:

  ```
   LOG  [BackgroundRemover] Using Apple Vision (free, on-device)
  ```

- **НЕ должны видеть:**
  ```
  LOG  [BackgroundRemover] Using Pixian.ai API (cloud, paid)
  ```

## ❌ Если что-то не работает

### Вариант 1: Модуль не загружается

```bash
# Логи показывают:
ERROR  [SubjectLifter] Native module not available
```

**Решение:**

```bash
# Остановите текущую сборку (Ctrl+C в терминале)
rm -rf ios
npx expo prebuild -p ios --clean
npx expo run:ios
```

### Вариант 2: Используется Pixian вместо Apple Vision

```bash
# Логи показывают:
LOG  [BackgroundRemover] Using Pixian.ai API
```

**Проверьте:**

1. iOS версия симулятора должна быть 16.0+
2. На реальном устройстве в логах должно быть `Apple Vision availability: true`

Если `false` - проверьте логи запуска:

```bash
grep -A5 "SubjectLifter" # в терминале с логами
```

## 🎯 Ожидаемый результат

### ✅ Успех выглядит так:

1. **В логах при запуске:**

   ```
   [SubjectLifter Plugin] ✅ Added SubjectLifter pod to Podfile
   [SubjectLifter Plugin] ✅ Vision.framework added
   ```

2. **В логах приложения:**

   ```
   [SubjectLifter] Native module loaded successfully!
   [BackgroundRemover] Apple Vision availability: true
   ```

3. **При удалении фона:**

   ```
   [BackgroundRemover] Using Apple Vision (free, on-device)
   ```

4. **Фон удаляется за 0.5-2 секунды** (не 5-10 как с Pixian)

## 📞 Если нужна помощь

1. Скопируйте логи из терминала (от начала сборки)
2. Найдите строки с `[SubjectLifter]` и `[BackgroundRemover]`
3. Покажите мне эти логи

## 🎉 Готово!

После успешной сборки и тестирования у вас будет:

- ✅ Полностью автоматизированная интеграция Apple Vision
- ✅ Бесплатное удаление фона на iOS 16+
- ✅ Автоматический fallback на Pixian для Android/старых iOS
- ✅ Настройка воспроизводится одной командой

---

**Следующие шаги после тестирования:**

Если всё работает, можно коммитить:

```bash
git add .
git commit -m "feat: Apple Vision background removal (automated)"
git push
```
