# 🤖 Запуск Obrazz на Android эмуляторе

## 📋 Шаг 1: Установка Android Studio (если еще не установлено)

1. Скачай Android Studio: https://developer.android.com/studio
2. Установи с настройками по умолчанию
3. При первом запуске выбери "Standard" installation

## 🔧 Шаг 2: Настройка Android SDK

1. Открой Android Studio
2. Зайди в **Tools → SDK Manager** (или **Android Studio → Settings → Appearance & Behavior → System Settings → Android SDK**)
3. Во вкладке **SDK Platforms** убедись что установлены:
   - ✅ Android 14.0 (API Level 34) — основная версия для проекта
   - ✅ Android 13.0 (API Level 33)
   - ✅ Android 12.0 (API Level 31)

4. Во вкладке **SDK Tools** убедись что установлены:
   - ✅ Android SDK Build-Tools
   - ✅ Android SDK Command-line Tools
   - ✅ Android SDK Platform-Tools
   - ✅ Android Emulator
   - ✅ Google Play Services

5. Нажми **Apply** → **OK**

## 📱 Шаг 3: Создание Android эмулятора

1. В Android Studio зайди в **Tools → Device Manager**
2. Нажми **Create Device**
3. Выбери устройство:
   - Рекомендую: **Pixel 6** или **Pixel 7** (современные устройства с хорошей производительностью)
   - Нажми **Next**
4. Выбери системный образ:
   - **Tiramisu (API Level 33)** или **UpsideDownCake (API Level 34)**
   - Если не скачан — нажми **Download**
   - Нажми **Next**
5. Настройки эмулятора:
   - AVD Name: `Pixel_6_API_33` (или любое имя)
   - Startup orientation: **Portrait**
   - Enable Device Frame: ✅
   - RAM: 2048 MB (минимум, можно больше если позволяет Mac)
6. Нажми **Finish**

## 🚀 Шаг 4: Открытие проекта в Android Studio

### Вариант A: Через терминал (рекомендуется для Expo)

```bash
cd /Users/artfrost/Projects/obrazz

# Запустить эмулятор отдельно (опционально)
# Сначала посмотри список эмуляторов
emulator -list-avds

# Запусти эмулятор по имени
emulator -avd Pixel_6_API_33 &

# Подожди пока эмулятор загрузится (30-60 секунд)

# Запусти приложение
npx expo run:android
```

### Вариант B: Через Android Studio

1. Открой Android Studio
2. Выбери **File → Open**
3. Перейди в: `/Users/artfrost/Projects/obrazz/android`
4. Нажми **Open**
5. Подожди пока Gradle синхронизируется (может занять 5-10 минут при первом запуске)
6. Если появится предложение обновить Gradle plugin — нажми **Update**

## ▶️ Шаг 5: Запуск приложения

### Из Android Studio:

1. В правом верхнем углу выбери эмулятор из выпадающего списка
2. Нажми зеленую кнопку **Run** (▶️) или **Shift + F10**
3. Приложение соберется и запустится на эмуляторе

### Из терминала (Expo CLI):

```bash
# В папке проекта
cd /Users/artfrost/Projects/obrazz

# Запустить Metro bundler + сборку Android
npx expo run:android

# Или если хочешь просто запустить Metro
npx expo start

# Затем в браузере нажми "a" для запуска на Android
```

## 🔍 Проверка что эмулятор запущен

```bash
# Проверить подключенные устройства
adb devices

# Должно показать что-то вроде:
# List of devices attached
# emulator-5554	device
```

## 🐛 Возможные проблемы

### Проблема: "ANDROID_HOME is not set"

**Решение:**

Добавь в `~/.zshrc` (или `~/.bashrc`):

```bash
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$ANDROID_HOME/tools
export PATH=$PATH:$ANDROID_HOME/tools/bin
```

Затем:

```bash
source ~/.zshrc
```

### Проблема: "Gradle sync failed"

**Решение:**

```bash
cd /Users/artfrost/Projects/obrazz/android
./gradlew clean
cd ..
npx expo run:android
```

### Проблема: "No space left on device" во время сборки

**Решение:**

```bash
# Очистить кеш Gradle
rm -rf ~/.gradle/caches/

# Очистить Android build
cd /Users/artfrost/Projects/obrazz/android
./gradlew clean
cd ..
```

### Проблема: Эмулятор тормозит

**Решение:**

1. В Device Manager → выбери свой эмулятор → Edit
2. Увеличь RAM до 4096 MB (если позволяет Mac)
3. В Emulated Performance выбери **Hardware - GLES 2.0**

### Проблема: "Build failed" с ошибками в native модулях

**Решение:**

```bash
# Пересобрать native modules
cd /Users/artfrost/Projects/obrazz
rm -rf node_modules
npm install
npx expo prebuild --clean
npx expo run:android
```

## ✅ Что должно получиться

После успешного запуска:

1. Metro bundler запустится в терминале
2. Android эмулятор откроется (если не был открыт)
3. Приложение Obrazz установится и запустится
4. Ты увидишь Welcome screen или Home screen (если уже авторизован)

## 🎨 Проверь Android-специфичные UI компоненты

После запуска проверь что работает:

- ✅ **Floating Tab Bar** внизу экрана (Material Design 3 стиль)
- ✅ **Анимированные иконки** в Tab Bar (outline → filled при нажатии)
- ✅ **Glass кнопки** имеют ripple effect при нажатии
- ✅ **Context Menu** открывается как bottom sheet (долгое нажатие на карточку в библиотеке)
- ✅ **Segment Control** в Library имеет animated indicator
- ✅ **FAB** (плюс) правильно позиционирован над Tab Bar
- ✅ **Dark mode** переключается корректно (Settings → Appearance)

## 📸 Hot Reload

Пока приложение запущено, ты можешь:

- Редактировать код
- Нажать **R** в терминале Metro для reload
- Нажать **Cmd + M** в эмуляторе для Dev Menu
- Включить **Fast Refresh** для автоматического обновления

## 🚀 Быстрый старт (TL;DR)

```bash
# 1. Открой эмулятор (если еще не открыт)
emulator -list-avds
emulator -avd Pixel_6_API_33 &

# 2. Запусти приложение
cd /Users/artfrost/Projects/obrazz
npx expo run:android

# 3. Подожди сборку (первый раз долго, потом быстрее)
# 4. Profit! 🎉
```

---

**Нужна помощь?** Если что-то не работает — скажи какая ошибка появляется и на каком шаге!
