<!DOCTYPE html>
<html lang="ru" class="h-full bg-white">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="csrf-param" content="authenticity_token" />
  <meta name="csrf-token" content="<%= form_authenticity_token %>" />
  
  <title><%= content_for(:title) || 'Obrazz - Вход' %></title>
  
  <!-- Favicon -->
  <link rel="icon" type="image/png" href="/favicon.png">
  
  <!-- Tailwind CSS -->
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  
  <!-- Custom styles for Obrazz design system -->
  <style>
    :root {
      --obrazz-primary: #1A1A2E;
      --obrazz-text: #000000;
      --obrazz-text-secondary: #666666;
      --obrazz-text-muted: #999999;
      --obrazz-bg: #FFFFFF;
      --obrazz-input-bg: #F5F5F5;
      --obrazz-border: #E5E5E5;
      --obrazz-error: #FF3B30;
      --obrazz-success: #34C759;
    }
    
    .obrazz-input {
      background-color: var(--obrazz-input-bg);
      border: none;
      border-radius: 12px;
      padding: 16px;
      font-size: 16px;
      color: var(--obrazz-text);
      transition: box-shadow 0.2s ease;
    }
    
    .obrazz-input:focus {
      outline: none;
      box-shadow: 0 0 0 2px var(--obrazz-primary);
    }
    
    .obrazz-input::placeholder {
      color: var(--obrazz-text-muted);
    }
    
    .obrazz-btn-primary {
      background-color: var(--obrazz-primary);
      color: white;
      border-radius: 30px;
      padding: 16px 24px;
      font-weight: 600;
      letter-spacing: 1px;
      text-transform: uppercase;
      font-size: 14px;
      transition: opacity 0.2s ease;
    }
    
    .obrazz-btn-primary:hover {
      opacity: 0.9;
    }
    
    .obrazz-btn-secondary {
      background-color: transparent;
      color: var(--obrazz-primary);
      border: 1.5px solid var(--obrazz-primary);
      border-radius: 30px;
      padding: 16px 24px;
      font-weight: 600;
      font-size: 14px;
      transition: background-color 0.2s ease;
    }
    
    .obrazz-btn-secondary:hover {
      background-color: var(--obrazz-input-bg);
    }
    
    .obrazz-social-btn {
      background-color: var(--obrazz-input-bg);
      border-radius: 28px;
      width: 56px;
      height: 56px;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: background-color 0.2s ease;
    }
    
    .obrazz-social-btn:hover {
      background-color: #EBEBEB;
    }
  </style>
  
  <!-- Turbo & Stimulus -->
  <%= javascript_importmap_tags %>
</head>
<body class="h-full bg-white">
  <div class="flex min-h-full flex-col justify-center px-8 py-12 sm:px-6 lg:px-8">
    <!-- Logo -->
    <div class="sm:mx-auto sm:w-full sm:max-w-md text-center mb-12">
      <h1 class="text-4xl font-bold tracking-[3px] text-[#1A1A2E]">OBRAZZ</h1>
      <p class="mt-2 text-sm text-[#666666]">Your Personal Stylist</p>
    </div>

    <!-- Flash Messages -->
    <div class="sm:mx-auto sm:w-full sm:max-w-md">
      <% if notice.present? %>
        <div class="mb-6 rounded-xl bg-[#F5F5F5] p-4 border-l-4 border-[#34C759]">
          <div class="flex items-center">
            <svg class="h-5 w-5 text-[#34C759] mr-3" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd" />
            </svg>
            <p class="text-sm font-medium text-[#1A1A2E]"><%= notice %></p>
          </div>
        </div>
      <% end %>
      
      <% if alert.present? %>
        <div class="mb-6 rounded-xl bg-[#FFF5F5] p-4 border-l-4 border-[#FF3B30]">
          <div class="flex items-center">
            <svg class="h-5 w-5 text-[#FF3B30] mr-3" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.28 7.22a.75.75 0 00-1.06 1.06L8.94 10l-1.72 1.72a.75.75 0 101.06 1.06L10 11.06l1.72 1.72a.75.75 0 101.06-1.06L11.06 10l1.72-1.72a.75.75 0 00-1.06-1.06L10 8.94 8.28 7.22z" clip-rule="evenodd" />
            </svg>
            <p class="text-sm font-medium text-[#FF3B30]"><%= alert %></p>
          </div>
        </div>
      <% end %>
    </div>

    <!-- Content -->
    <div class="sm:mx-auto sm:w-full sm:max-w-md">
      <%= yield %>
      
      <!-- Footer -->
      <div class="mt-12 text-center">
        <a href="https://obrazz.app" target="_blank" rel="noopener noreferrer" class="text-sm text-[#666666] hover:text-[#1A1A2E] transition-colors">
          ← Вернуться на главный сайт
        </a>
        <p class="mt-4 text-sm text-[#999999]">
          &copy; <%= Date.current.year %> Obrazz. Все права защищены.
        </p>
      </div>
    </div>
  </div>
</body>
</html>
